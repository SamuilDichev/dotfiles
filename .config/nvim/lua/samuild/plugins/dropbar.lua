return {
    -- TODO: Requires nvim 0.10 (nightly) but its calling get_clients when that doesn't exist yet
    -- TODO: Because nightly on snap isn't latest nightly, but from a year ago...
    --
    -- "Bekaboo/dropbar.nvim",
    -- event = { "BufReadPre", "BufNewFile" },
    -- dependencies = {
    --     "nvim-telescope/telescope-fzf-native.nvim",
    --     "nvim-tree/nvim-web-devicons",
    -- },
}
