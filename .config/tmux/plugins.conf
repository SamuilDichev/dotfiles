set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'catppuccin/tmux'
# set -g @plugin 'tmux-plugins/tmux-cpu'  # requires double sourcing of conf file, idk why
# set -g @plugin 'tmux-plugins/tmux-sensible'  # most of this thing's binds are in my conf
# set -g @plugin 'tmux-plugins/tmux-battery'  # requires double sourcing of conf
