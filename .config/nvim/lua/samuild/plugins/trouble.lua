return {
    "folke/trouble.nvim",
    dependencies = { "nvim-tree/nvim-web-devicons" },
    lazy = false,
    opts = {
        height = 15,
        padding = false,
        mode = "document_diagnostics",
        auto_close = true,
    }
}
